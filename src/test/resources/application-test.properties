spring.datasource.url=jdbc:h2:mem:neatly_test;MODE=PostgreSQL;DATABASE_TO_LOWER=TRUE;DEFAULT_NULL_ORDERING=HIGH
spring.datasource.username=sa
spring.datasource.password=
spring.datasource.driver-class-name=org.h2.Driver

spring.jpa.hibernate.ddl-auto=create-drop
spring.jpa.properties.hibernate.jdbc.time_zone=UTC

# JWT for OAuth2 resource server tests (HS256, >= 256 bits)
supabase.jwt.secret=test-jwt-secret-for-neatly-ci-only-min-32-chars

stripe.secret-key=
stripe.webhook-secret=

gemini.api-key=
